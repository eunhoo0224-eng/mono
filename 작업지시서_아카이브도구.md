# 사진 아카이브 정리 도구 — 작업 지시서 v0.1

> **읽는 주체**: Claude Code
> **언제 여나**: `photo-archive/` 안의 사진 파일을 옮기거나, 이름을 바꾸거나, 웹용으로 내보낼 때.
> **짝 문서**: `사진아카이브_분류체계.md` — **이름·폴더 규칙의 원본은 그 파일이다.** 여기 복사하지 않는다. 두 문서가 어긋나면 분류체계 쪽이 맞다.
> **지위**: ⚠잠정. 첫 촬영 건을 돌린 뒤 고친다.

---

## 1. 만들 것

로컬에서 도는 CLI 하나. 촬영 원본 폴더를 받아서 규칙에 맞는 촬영 건 폴더로 정리하고, 색인에 기록하고, 웹 업로드용 사본을 만든다.

언어·라이브러리는 정하지 않는다. 아래 계약과 금지 사항만 지키면 된다.

---

## 2. 절대 하지 말 것

**이 절이 나머지 전부보다 우선한다.** 사진은 다시 찍을 수 없다.

- **원본을 수정·이동·삭제하지 않는다.** 읽고 복사만 한다. 입력 폴더는 끝난 뒤에도 그대로여야 한다.
- **파일을 덮어쓰지 않는다.** 목적지에 같은 이름이 있으면 그 파일을 건너뛰고 기록한 뒤, 나머지를 계속 처리한다. 조용히 지나가지 않는다.
- **기본 동작은 dry-run이다.** 실제 쓰기는 `--apply`가 붙었을 때만 한다. 플래그 없이 돌리면 무엇을 할지만 출력한다.
- **번호를 다시 매기지 않는다.** 한 번 붙은 일련번호는 모든 단계에서 같다.
- **`index.csv`의 기존 행을 고치거나 지우지 않는다.** 추가만 한다.
- **트랙(C/P)을 추론하지 않는다.** 사람이 인자로 준 값만 쓴다. 없으면 실행을 멈추고 묻는다.
- **EXIF의 저작권·작가 정보를 지우지 않는다.** GPS만 지운다.
- **네트워크로 아무것도 보내지 않는다.**

---

## 3. 명령과 계약

각 명령은 입력·출력·불변식만 정한다. 구현 방식은 자유다.

### 3-1. `ingest`

원본 폴더를 촬영 건 폴더로 옮겨 담는다.

- **입력**: 원본 폴더 경로, 트랙(`C`|`P`), slug, (선택) 날짜
- **날짜**: 주지 않으면 EXIF 촬영일시에서 읽는다. 파일마다 다르면 **가장 이른 날**을 쓰고, 며칠에 걸쳐 있다는 사실을 출력한다.
- **출력**: `YYYYMMDD_[C|P]_slug/00_raw/` 아래에 규칙대로 이름 붙은 사본, 그리고 `index.csv`에 추가된 한 줄
- **번호 순서**: EXIF 촬영일시 오름차순. EXIF가 없는 파일은 원래 파일명 순으로 뒤에 붙인다.
- **불변식**: 입력 폴더의 파일 수 = 출력 폴더의 파일 수 + 건너뛴 수

### 3-2. `export`

고른 사진을 웹용으로 내보낸다.

- **입력**: 촬영 건 폴더 경로
- **읽는 곳**: `10_select/` — 비어 있으면 아무것도 하지 않고 그 사실을 알린다. `00_raw/`를 대신 읽지 않는다.
- **출력**: `20_web/`에 같은 이름·같은 번호의 사본
- **스펙**: 4절
- **불변식**: 출력 파일의 번호는 입력 파일의 번호와 같다

### 3-3. `check`

규칙 위반을 찾는다. 고치지는 않는다.

찾을 것:
- 이름 규칙에 안 맞는 폴더·파일
- `index.csv`에 있는데 폴더가 없는 건, 폴더는 있는데 색인에 없는 건
- 폴더 이름의 slug와 색인의 slug 불일치
- `count` 값과 `00_raw/`의 실제 파일 수 불일치
- 중복된 `id`
- `20_web/`에 있는데 `10_select/`에 없는 파일

### 3-4. `status`

촬영 건 목록을 `status` 칸 기준으로 요약해 보여준다.

---

## 4. 웹 export 스펙

| 항목 | 값 |
|---|---|
| 긴 변 | 2560px (`--hero` 지정 시 3840px) |
| 원본이 더 작을 때 | **키우지 않는다.** 원본 크기 유지 |
| 형식 | JPEG |
| 품질 | 80 |
| 색공간 | sRGB로 변환 |
| 메타데이터 | 저작권·작가 유지, GPS 제거, 나머지는 유지 |

색공간 변환은 프로파일을 무시하고 태그만 바꾸는 방식으로 하지 않는다. 실제로 변환해야 한다.

---

## 5. 완료 판정

아래가 전부 통과해야 끝난 것이다.

1. 사진 5장이 든 폴더로 `ingest`를 `--apply` 없이 돌리면, 아무 파일도 생기지 않고 예정 작업만 출력된다.
2. 같은 명령을 `--apply`로 돌리면 5장이 복사되고, 입력 폴더는 그대로다.
3. 같은 명령을 한 번 더 `--apply`로 돌리면 5장 모두 건너뛰고, 새 행이 추가되지 않는다.
4. `check`가 방금 만든 건에 대해 위반 0건을 낸다.
5. `export` 결과물의 긴 변이 2560px 이하이고, GPS가 없고, 저작권 정보가 남아 있다.
6. 파일 이름을 하나 손으로 망가뜨린 뒤 `check`를 돌리면 그 파일을 잡아낸다.

---

## 6. 물어보고 진행할 것

추측하지 말고 멈춰서 물을 것:

- 트랙(C/P)이 인자로 안 왔을 때
- EXIF 날짜가 3일 이상 벌어져 있을 때 — 한 건인지 나눌 건인지
- `index.csv`가 없을 때 — 새로 만들지, 경로가 틀린 건지
- 위 규칙끼리 부딪치는 상황을 발견했을 때

---

## 7. 이 문서의 관리

- 규칙 추가는 **실제로 어긋난 뒤**에만 한다. 미리 채우지 않는다.
- 한 번도 발동하지 않은 규칙은 은퇴 후보로 표시한다.
- 구현 방법(라이브러리·구조·패턴)은 여기 적지 않는다. 지키라고 적을수록 결과가 나빠진다.
