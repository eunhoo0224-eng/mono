# 사진 아카이브 분류 체계 v0.1 (⚠잠정)

> 김은후 / silverhoo · 2026-07-22
> **목적**: 제목을 정하기 전에, 사진을 넣고 찾을 수 있게 한다.
> **지위**: 아직 실사용 검증 전이다. 20건쯤 쌓은 뒤 어긋난 것만 고친다.

---

## 0. 세 가지 원칙

1. **한 층에는 한 축만.** 폴더는 오직 `트랙 → 촬영 건` 두 층이다. 나머지 구분(소속·대상·장비·인물)은 전부 태그로 간다.
2. **원본과 웹용을 절대 섞지 않는다.** 원본은 웹에 올라가지 않고, 웹용은 백업 대상이 아니다.
3. **제목은 지금 정하지 않는다.** 폴더와 파일 이름에는 식별자만 넣는다. 제목은 나중에 `index.csv`의 빈 칸에 채운다.

---

## 1. 폴더 구조

촬영 건 하나가 폴더 하나다. 그 안에 단계별 하위 폴더를 둔다.

```
photo-archive/
├─ index.csv                       ← 촬영 건 목록 (전체 색인)
│
├─ 20260710_C_studio-group/        ← 촬영 건 1
│   ├─ 00_raw/                     원본 (3~40MB). 백업 대상. 웹에 안 감
│   ├─ 10_select/                  고른 것. 원본 화질 유지
│   └─ 20_web/                     웹 업로드용 export
│
├─ 20260602_P_hangang-night/       ← 촬영 건 2
│   ├─ 00_raw/
│   ├─ 10_select/
│   └─ 20_web/
│
└─ ...
```

**왜 촬영 건별로 묶나**: 폴더 하나가 통째로 완결되어 있어야 옮기고, 백업하고, 나중에 통째로 들어내기 쉽다. 단계별로(전체 raw / 전체 web) 묶으면 반대가 된다 — 일괄 처리는 편하지만 건 단위로 다루기가 어려워진다. 아카이브는 계속 쌓이는 물건이라 건 단위를 택했다.

---

## 2. 이름 규칙

### 촬영 건 폴더

```
YYYYMMDD_[C|P]_slug
```

예: `20260710_C_studio-group` · `20260602_P_hangang-night`

- **날짜가 맨 앞** — 이름순 정렬이 곧 시간순 정렬이 된다.
- **slug는 영문 소문자 + 하이픈** — 나중에 이 slug가 그대로 URL이 된다. 한글을 쓰면 주소창에서 `%ED%95%9C%EA%B8%80` 꼴로 깨진다.
- 여러 날에 걸친 촬영은 **시작일**을 쓴다.

### 파일

```
YYYYMMDD_[C|P]_slug_###.확장자
```

예: `20260710_C_studio-group_014.jpg`

- 폴더 이름과 겹치지만 그대로 둔다. 파일이 폴더를 벗어나도 혼자 설명되기 때문. 대신 이름이 길어진다 — 감수할 값.
- **번호는 절대 다시 매기지 않는다.** `00_raw`의 014번은 `10_select`에서도 `20_web`에서도 014번이다. 번호가 원본으로 돌아가는 끈이다.

---

## 3. 트랙 판정 — C냐 P냐

기준은 **장소가 아니라 수신자**다. "어디서 찍었나"가 아니라 **"이 사진을 기다린 사람이 있었나"**로 가른다.

| | 트랙 | 판정 |
|---|---|---|
| **C** (Commissioned) | 받을 사람이 정해져 있던 촬영 | 인감 단체사진, 행사 스냅, 요청받은 인물 촬영 |
| **P** (Personal) | 아무도 기다리지 않던 촬영 | 출사, 풍경, 내가 먼저 기획한 컨셉 |

**인감(UNIST 사진 소모임 STUDIO)은 폴더 층에 넣지 않는다.** 소속은 트랙과 다른 축이기 때문이다. 여기 넣으면 원칙 1이 깨지고, 지금 사이트가 걸린 지점("개인/인감"과 "피사체/풍경"이 한 줄에 섞인 것)을 그대로 반복하게 된다. 대신 `studio` 태그로 붙인다.

- 인감 **단체사진** → `C` + 태그 `studio` (단원들이 결과물을 기다렸으므로)
- 인감 **컨셉사진** → 기획을 누가 했느냐로 갈린다. 내가 먼저 하자고 했으면 `P`, 소모임 과제였으면 `C`. 둘 다 태그 `studio`.

⚠ 애매한 건이 다섯 개 넘게 쌓이면 이 기준이 잘못된 것이다. 그때 기준을 고친다.

---

## 4. 태그 (통제 목록)

태그는 **Lightroom 키워드**로 파일에 직접 박는다. 파일을 따라다니고 검색되기 때문. 목록은 짧게 유지한다 — 늘어나면 안 쓰게 된다.

- **소속**: `studio`
- **대상**: `portrait` · `group` · `landscape` · `concept` · `event`

여기 없는 태그가 필요해지면 그때 추가하되, **추가 이유를 index.csv의 note에 적는다.**

---

## 5. index.csv

촬영 건 하나가 한 줄이다. 사진 낱장은 여기 안 들어간다.

```csv
id,date,track,slug,place,tags,count,status,title_ko,title_en,note
001,2026-07-10,C,studio-group,UNIST,studio;group,46,raw,,,
002,2026-06-02,P,hangang-night,서울,landscape,25,selected,,,
```

| 칸 | 넣는 것 |
|---|---|
| `id` | 3자리 일련번호. 한 번 준 번호는 재사용하지 않는다 |
| `date` | 촬영일 (여러 날이면 시작일) |
| `track` | `C` 또는 `P` |
| `slug` | 폴더 이름의 slug와 반드시 일치 |
| `place` | 장소 (자유 기입) |
| `tags` | 세미콜론으로 구분 |
| `count` | 원본 장수 |
| `status` | `raw` → `selected` → `web` → `published` |
| `title_ko` / `title_en` | **지금은 비워 둔다.** 제목 단계에서 채운다 |
| `note` | 기억해야 할 것, 판정이 애매했던 이유 |

---

## 6. 웹 export 스펙

`20_web/`에 넣을 때 쓰는 값이다.

| 항목 | 값 | 이유 |
|---|---|---|
| 긴 변 | **2560px** | 1280px 컨테이너의 2배. 일반 레티나를 덮는다 |
| 대표작만 | 3840px | Adobe Portfolio가 만드는 최대 렌디션 폭 |
| 형식·품질 | JPEG, 품질 **80** | 대략 300~600KB로 떨어진다 |
| 색공간 | **sRGB 필수** | Adobe RGB로 내보내면 브라우저에서 채도가 죽는다 |
| 샤프닝 | 화면용 · 표준 | 축소하면 무뎌지므로 |
| 메타데이터 | 저작권 유지, **GPS 제거** | 촬영 위치가 그대로 노출되는 사고 방지 |

**페이지당 장수는 15~25장으로 제한한다.** 46장 × 400KB면 한 페이지가 18MB다. export 스펙만으로는 이 문제가 풀리지 않는다.

---

## 7. 하지 말 것

- 원본(3~40MB)을 그대로 웹에 올리지 않는다 — 저장공간과 업로드 시간만 먹고 화면에서는 이득이 없다.
- 웹 플랫폼을 백업으로 쓰지 않는다 — Adobe Portfolio에서 원본 해상도를 되돌려 받지 못한다는 보고가 반복된다.
- 번호를 다시 매기지 않는다.
- 폴더 이름에 한글을 쓰지 않는다.
- 태그를 폴더로 승격시키지 않는다.

---

## 8. 아직 안 정한 것

1. **제목 규칙** — 이 체계가 20건쯤 돌아간 뒤에 정한다.
2. **Selected 층의 선별 기준** — 사진을 봐야 정해진다.
3. **인감 컨셉사진의 트랙** — 실제 건을 넣어 보며 확정한다.
